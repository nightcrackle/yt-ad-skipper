# Normalize line endings to LF in the repo; let git handle checkout conversion.
* text=auto eol=lf

# Treat these as binary (no line-ending conversion, no text diffs).
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
